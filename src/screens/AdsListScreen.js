import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const AdsListScreen = props => {
	return (
		<View>
			<Text>AdsListScreen</Text>
		</View>
	);
};

const styles = StyleSheet.create({});

export default AdsListScreen;
